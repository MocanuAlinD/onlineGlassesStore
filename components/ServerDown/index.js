import React from 'react'
import {Container, ServerText, ServerImage} from './ServerDown.style'



const ServerDown = () => {
    return (
        <Container>
            <ServerText>Product unavailable for the moment. The server is temporary down.</ServerText>
        </Container>
    )
}

export default ServerDown;
